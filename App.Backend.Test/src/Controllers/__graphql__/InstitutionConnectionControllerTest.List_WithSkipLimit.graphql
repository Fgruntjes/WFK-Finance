query institutionConnections($skipLimitArgs: Input_SkipLimitArgs) {
    institutionConnections(skipLimitArgs: $skipLimitArgs) {
        items {
            externalId
        }
        totalCount
    }
}